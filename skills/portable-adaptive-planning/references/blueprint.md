# Planning blueprint

Use this projection after the plan is `FINAL` and before execution. Carry only the locked plan needed by the Builder and Reviewer.

```text
AS OF
<short verbatim fragment of the latest operator instruction the plan accounts for>

ASKED
<the operator's exact requested outcome>

GOAL
<the accepted agent restatement>

DELIVERABLE
<what will exist>

SOURCE HOME
<authorized project, repository, or surface> | EVIDENCE: <current authority>

REQUIREMENTS / PLAN
1. <required result or step>
2. <required result or step>

BOUNDARIES
<load-bearing exclusions or limits>

DONE
<the smallest observable acceptance condition>

REOPEN IF
<the objective, deliverable, source home, load-bearing boundary, acceptance
condition, or blueprint currency must change>
```

`AS OF` is a short verbatim fragment of the latest operator instruction the plan accounts for. A newer or materially different operator instruction reopens planning before Builder or Reviewer progression. A prior `GO` does not override a later operator correction.

Omit `SOURCE HOME` and `BOUNDARIES` when they carry no load-bearing information.

The Builder implements inside this blueprint. The Reviewer checks objective fidelity first, then correctness and acceptance evidence.

Do not pass planning transcripts, readback history, discarded alternatives, or non-load-bearing discussion downstream.
